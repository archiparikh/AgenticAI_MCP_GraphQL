# GraphQL endpoint (required)
GRAPHQL_ENDPOINT=http://localhost:4000/graphql

# API authentication token (leave blank if the API is public)
GRAPHQL_API_TOKEN=

# Auth header scheme: Bearer | Token | Basic | …
GRAPHQL_AUTH_SCHEME=Bearer

# Request timeout in seconds
GRAPHQL_TIMEOUT=30
